#!/usr/bin/env bash

# Copy/link this script to .git/hooks with (ln -s ../../.pre-commit .git/hooks/pre-commit)

ENV_NAME="bioquant_devel"
ENV_FILE="conda_env.yml"

conda env export -n ${ENV_NAME} -f ${ENV_FILE}
git add ${ENV_FILE}

